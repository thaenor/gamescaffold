<?php $__env->startSection('content'); ?>
    <div class="page-header">
        <h1>Rewards</h1>
    </div>


    <div class="row">
        <div class="col-md-12">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>TITLE</th>
                        <th>ASSIGNED_USER</th>
                        <th>WINNER</th>
                        <th class="text-right">OPTIONS</th>
                    </tr>
                </thead>

                <tbody>

                <?php foreach($rewards as $reward): ?>
                <tr>
                    <td><?php echo e($reward->id); ?></td>
                    <td><?php echo e($reward->title); ?></td>
                    <td><?php echo e($reward->assigned_user); ?></td>
                    <td><?php echo e($reward->winner); ?></td>

                    <td class="text-right">
                        <a class="btn btn-primary" href="<?php echo e(route('rewards.show', $reward->id)); ?>">View</a>
                        <a class="btn btn-warning " href="<?php echo e(route('rewards.edit', $reward->id)); ?>">Edit</a>
                        <form action="<?php echo e(route('rewards.destroy', $reward->id)); ?>" method="POST" style="display: inline;" onsubmit="if(confirm('Delete? Are you sure?')) { return true } else {return false };"><input type="hidden" name="_method" value="DELETE"><input type="hidden" name="_token" value="<?php echo e(csrf_token()); ?>"> <button class="btn btn-danger" type="submit">Delete</button></form>
                    </td>
                </tr>

                <?php endforeach; ?>

                </tbody>
            </table>

            <a class="btn btn-success" href="<?php echo e(route('rewards.create')); ?>">Create</a>
        </div>
    </div>


<?php $__env->stopSection(); ?>
<?php echo $__env->make('layout', array_except(get_defined_vars(), array('__data', '__path')))->render(); ?>